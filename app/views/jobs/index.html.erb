<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl mb-6">Jobs</h1>
    <%= link_to 'New job', new_job_path, data: {turbo_frame: dom_id(Job.new) },
                class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>
  <div id="jobs" class="min-w-full my-3">
    <%= turbo_frame_tag Job.new %>
    <%= render @jobs %>
  </div>
</div>
